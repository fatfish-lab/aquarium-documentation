meta {
  name: Delete a personal access token
  type: http
  seq: 20
}

delete {
  url: {{protocol}}://{{url}}/{{version}}/users/me/tokens/:id
  body: none
  auth: inherit
}

params:path {
  id: {{tokenId}}
}

docs {
  No description
}
