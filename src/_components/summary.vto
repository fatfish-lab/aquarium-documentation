<summary class="shortcut {{ if active }}active{{ /if }}">
  {{ comp.shortcut(it) }}
  {{# {{ if it }}
    {{ comp.shortcut(it) }}
  {{ else }}
    <span>{{ capitalize(slug) }}</span>
  {{ /if }} #}}
</summary>
